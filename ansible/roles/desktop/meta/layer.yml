# Machine-readable manifest for the Converge UI (see ansible/roles/README.md).
name: Desktop
description: (currently empty — kept as a slot for future desktop-only tasks)
profiles: [client]
requires: [core]
# No tasks: the kitty theme clones this role used to own are actually
# unconditional chezmoi externals (.chezmoiexternal.toml.tmpl), applied on
# every machine regardless of this flag — see tasks/main.yml.
tasks: []
