# Managed by chezmoi — rendered from .chezmoi.toml.tmpl data.
# Consumed by ansible-pull via `include_vars` in roles/core (see core/tasks/main.yml).
# Do not edit by hand: change the prompt answer (chezmoi init) or .chezmoi.toml.tmpl.
pixi_home: {{ .pixi.home | quote }}
